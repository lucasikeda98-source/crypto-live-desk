name: Quality

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deterministic-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    strategy:
      fail-fast: false
      matrix:
        node-version: [22, 24]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm
      - name: Install locked dependencies
        run: npm ci
      - name: Audit locked dependencies
        run: npm audit --audit-level=low
      - name: Run deterministic tests with coverage floors
        run: npm run test:coverage

  # LIMITACAO DOCUMENTADA (OPS-003/REV-CC-01): o smoke busca klines direto da Binance e
  # runners hospedados do GitHub podem receber HTTP 451 (geo-bloqueio) independente do codigo.
  # Por isso o job e ADVISORY (continue-on-error) ate os dados serem proxyados pela camada /api
  # com fixture de CI. Uma segunda tentativa absorve oscilacao curta das fontes. O gate
  # bloqueante deterministico e o job deterministic-tests acima.
  browser-smoke:
    runs-on: windows-latest
    timeout-minutes: 15
    continue-on-error: true
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Use Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
      - name: Install locked dependencies
        run: npm ci
      - name: Run browser smoke (Edge headless, 1 retry)
        shell: pwsh
        env:
          EDGE_PATH: C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe
        run: |
          node scripts/browser-smoke.cjs
          if ($LASTEXITCODE -ne 0) {
            Write-Host 'Primeira execucao falhou; tentando novamente (fontes externas podem oscilar).'
            node scripts/browser-smoke.cjs
          }
          exit $LASTEXITCODE
